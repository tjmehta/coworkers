// TODO

# coworkers
RabbitMQ consumer service framework

```js
var app = require('coworkers')()

app.use(...)
app.use(...)
app.use(...)
app.consume('hello', ...)
app.consume('hello', ...)
app.consume('hello', ...)

app.listen(...)
```
